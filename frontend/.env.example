# URL base de la API backend, tal como la alcanza el NAVEGADOR (no la red interna de Docker).
# Tanto en ejecución local como con Docker Compose, el backend publica su puerto en el host,
# por lo que esta URL es la misma en ambos casos.
VITE_API_URL=http://localhost:4000/api
